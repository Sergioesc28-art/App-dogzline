import 'package:flutter/material.dart';

class ApartadoScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Apartado Screen'),
      ),
      body: Center(
        child: Text('Apartado Screen Content'),
      ),
    );
  }
}